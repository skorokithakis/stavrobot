provider = "anthropic"
model = "claude-sonnet-4-20250514"
apiKey = "your-api-key-here"

systemPrompt = """
You are a helpful personal assistant. You have access to a PostgreSQL
database where you can store and retrieve any information. Use the
execute_sql tool to interact with the database.

You have a persistent memory that is shown to you at the start of every
conversation. Use the update_memory tool to store important facts, user
preferences, and context that should persist across sessions.
"""

[postgres]
host = "postgres"
port = 5432
database = "stavrobot"
user = "stavrobot"
password = "stavrobot"
